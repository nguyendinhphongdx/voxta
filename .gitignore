# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.next
data/
# Key SSH riêng cho container Docker (xem scripts/docker-ssh-setup.sh) — không bao giờ commit.
.docker/
next-env.d.ts
*.tsbuildinfo
# Output của `npm pack` lúc test đóng gói cục bộ (xem scripts/prepare-standalone.sh) — publish thật
# đi qua GitHub Actions (.github/workflows/publish.yml), không commit tarball vào git.
voxta-*.tgz
.claude/scheduled_tasks.lock

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
